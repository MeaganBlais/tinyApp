<!DOCTYPE html>
<html lang="en">
<head>
    <title>TinyApp</title>
</head>
<body>
  <header>
    <% include partials/_header %>
  </header>
  <% if (username === 'guest') { %>

  <% } else { %>
  <ul>
      <% for (url in urls) { %>
          <li>
            <form action= "<%= `/urls/${urls[url].shortURL}/delete` %>" method="POST" style="margin:150px;">
                <label> <%= urls[url].longURL %> <%= urls[url].shortURL %> </label>
                <a href= "/urls/<%=urls[url].shortURL%>"> edit </a>
                <input type="submit" value="Delete">
            </form>
          </li>
      <% } %>
  </ul>
  <% } %>
</body>
</html>
