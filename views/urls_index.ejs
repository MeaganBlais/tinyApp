<!DOCTYPE html>
<html lang="en">
<head>
    <title>TinyApp</title>
</head>
<body>
  <header>
    <% include partials/_header %>
  </header>
  <ul>
    <li>
        <form action="/urls_index" method="POST" style="">
            <label for="url">Enter url: </label>
            <input id="url" type="text" name="">
            <input type="submit" value="Submit">
        </form>
    </li>
      <% for (url in urls) { %>
        <li>
          <form action= <%= `/urls/${url}/delete` %> method="POST" style="margin:150px;">
              <label> <%= url %> <%= urls[url] %> </label>
              <a href= "/urls/<%=url%>"> edit </a>
              <input type="submit" value="Delete">
          </form>
          <% } %>
        </li>

        <!-- Removed since the 'delete' form includes all the urls
        <li>URL    shortURL</li>
        <% for (key in urls) { %>
        <li><%=key%>   <%=urls[key]%></li>
        <% } %> -->

  </ul>
</body>
</html>
